
╔══════════════════════════════════════════════════════════════════════════╗
║          GEN 3 TROUBLESHOOTING GUIDE - If Issues Persist                 ║
║                         December 18, 2025, 7:10 PM EET                    ║
╚══════════════════════════════════════════════════════════════════════════╝

This guide covers what to do if Q-values still explode or training fails
despite the Gen 3 architectural fixes.

═══════════════════════════════════════════════════════════════════════════
⚠️ SYMPTOM 1: Q-Values Still Exceed ±30 by Episode 1000
═══════════════════════════════════════════════════════════════════════════

Diagnostic Questions:
─────────────────────
1. Did you delete old checkpoints before training?
   If NO: Old diverged weights might be loaded!

2. Is LayerNorm actually being applied?
   Check: Print x.mean() and x.std() after LayerNorm
   Expected: mean ≈ 0, std ≈ 1

3. Is output scaling working?
   Check: Print q_values before and after ×0.1
   Expected: Before ~[-200,200], After ~[-20,20]

Potential Causes & Solutions:
──────────────────────────────

Cause 1: LayerNorm not initialized properly
───────────────────────────────────────────
Check in model.py:
```
self.feature_norm = nn.LayerNorm(4096)
```
Make sure this is in __init__ and applied in forward()!

Fix: Verify the line exists and is being called

Cause 2: Output scaling applied in wrong place
───────────────────────────────────────────────
Output scaling should be AFTER aggregation:
  WRONG: value = value * 0.1; advantage = advantage * 0.1
  RIGHT: q_values = (value + advantage - advantage_mean) * 0.1

Fix: Move scaling to very last line of forward()

Cause 3: Reward scaling not applied
────────────────────────────────────
Check in main.py after env.step():
  reward = reward / 100.0  # Must be here!

Fix: Add this line if missing

Cause 4: Learning rate still too high
──────────────────────────────────────
If Q-values > 30 but < 100 by Ep 1000:
  → Reduce LR further: 1e-6 → 5e-7 or 1e-7

Cause 5: Network initialization issue
──────────────────────────────────────
Rare, but possible if custom initialization:
  → Check that Conv/FC layers use default PyTorch init
  → Consider adding: self.apply(self._init_weights)

═══════════════════════════════════════════════════════════════════════════
⚠️ SYMPTOM 2: Loss Remains High (>20) Throughout Training
═══════════════════════════════════════════════════════════════════════════

Diagnostic Questions:
─────────────────────
1. What's the actual loss value?
   >100: Q-values are still exploding (see Symptom 1)
   20-50: Learning is happening but slow
   <20: Normal for very early training

2. Is loss decreasing at all?
   Yes, slowly: Expected with LR=1e-6
   No, flat: Problem with learning

Potential Causes & Solutions:
──────────────────────────────

Cause 1: Huber loss delta too large
────────────────────────────────────
Huber loss has a δ parameter (usually 1.0)
If TD errors are large, loss stays high

Fix: Reduce Huber delta in trainer.py:
  F.smooth_l1_loss(q_pred, q_target, beta=0.5)  # Was 1.0

Cause 2: Target network not updating
─────────────────────────────────────
Check that soft updates are actually happening

Fix: Add logging in update_target_network():
  print(f"Target updated at step {step_count}")

Cause 3: Batch contains only similar experiences
─────────────────────────────────────────────────
If buffer is small or episodes too similar:
  → Not enough diversity in training data

Fix: Increase MIN_BUFFER_SIZE to 5000 or 10000

Cause 4: Optimizer not stepping
────────────────────────────────
Rare but check that:
  optimizer.zero_grad()
  loss.backward()
  optimizer.step()

All three are being called!

═══════════════════════════════════════════════════════════════════════════
⚠️ SYMPTOM 3: Agent Not Learning (Reward Stays Flat/Negative)
═══════════════════════════════════════════════════════════════════════════

Diagnostic Questions:
─────────────────────
1. How many episodes have you trained?
   <1000: Too early to judge
   1000-3000: Should see some improvement
   >5000: Definitely a problem if still flat

2. What's the epsilon value?
   >0.5: Still exploring (normal)
   <0.1: Should be exploiting learned policy

3. Is loss decreasing?
   No: Not learning (see Symptom 2)
   Yes: Learning but policy not improving

Potential Causes & Solutions:
──────────────────────────────

Cause 1: Learning rate TOO low
───────────────────────────────
With LR=1e-6, learning is very slow
If after 5000 episodes no improvement:
  → Increase LR to 5e-6 or 1e-5
  → Monitor Q-values carefully!

Cause 2: Reward signal too weak
────────────────────────────────
After /100 scaling:
  Move: -0.005 (almost nothing)
  Capture: +0.05 (small)
  Win: +1.0 (good)

If agent never wins:
  → Never gets meaningful reward
  → Can't learn

Fix: Play against weaker opponent initially
  Or add intermediate rewards (king creation, etc.)

Cause 3: Exploration vs Exploitation
─────────────────────────────────────
Check epsilon decay schedule:
  EPSILON_DECAY = 2000 might be too fast
  Agent stops exploring before learning

Fix: Increase EPSILON_DECAY to 5000
  Or use ε_end = 0.1 instead of 0.05

Cause 4: Action space too large
────────────────────────────────
170 actions is large for checkers
If most are illegal most of the time:
  → Network struggles to differentiate

Fix: Consider reducing action space
  Or increase training time to 20k episodes

═══════════════════════════════════════════════════════════════════════════
⚠️ SYMPTOM 4: Training Crashes (NaN/Inf Values)
═══════════════════════════════════════════════════════════════════════════

Diagnostic Questions:
─────────────────────
1. When does it crash?
   Episode 1-10: Initialization issue
   Episode 100-500: Learning instability
   Episode 1000+: Rare, likely bug

2. What's NaN/Inf?
   Loss: Gradient explosion
   Q-values: Network output explosion
   Reward: Environment bug

Potential Causes & Solutions:
──────────────────────────────

Cause 1: Division by zero in LayerNorm
───────────────────────────────────────
If all features are identical:
  std = 0 → division by zero

Fix: LayerNorm has eps parameter:
  nn.LayerNorm(4096, eps=1e-5)  # Default should work

Cause 2: Gradient explosion despite clipping
─────────────────────────────────────────────
Very rare with clip=0.1

Fix: Reduce to 0.05 or even 0.01

Cause 3: Reward is NaN from environment
────────────────────────────────────────
Check environment returns valid rewards

Fix: Add assertion:
  assert not np.isnan(reward), "Reward is NaN!"

Cause 4: Learning rate scheduler (if using)
────────────────────────────────────────────
If you added LR decay:
  → Might decay to 0 or NaN

Fix: Remove scheduler or use fixed LR

═══════════════════════════════════════════════════════════════════════════
⚠️ SYMPTOM 5: Agent Learns But Loses to Random After Training
═══════════════════════════════════════════════════════════════════════════

Diagnostic Questions:
─────────────────────
1. Win rate against random?
   0-20%: Not learning proper strategy
   30-50%: Learning but inconsistent
   60-80%: Good but could be better
   >90%: Expected for 10k episodes

2. Does agent make captures?
   No: Serious problem
   Yes: Basic tactics learned

3. Does agent advance pieces?
   No: No long-term planning
   Yes: Understanding objectives

Potential Causes & Solutions:
──────────────────────────────

Cause 1: Network too conservative
──────────────────────────────────
With heavy scaling (Q×0.1, reward/100):
  → All Q-values might be very similar
  → Agent sees little difference between actions

Fix: Slightly reduce scaling:
  Output: ×0.2 instead of ×0.1
  Reward: /75 instead of /100

Cause 2: Evaluation uses different epsilon
───────────────────────────────────────────
If evaluating with ε > 0:
  → Agent takes random actions

Fix: Use ε = 0 (greedy) during evaluation

Cause 3: Model not in eval mode
────────────────────────────────
If BatchNorm or Dropout (not in your case):
  → Behavior differs between train/eval

Fix: Call model.eval() before evaluation

Cause 4: Training on different rewards than evaluation
───────────────────────────────────────────────────────
If reward function changed:
  → Agent optimized for old rewards

Fix: Retrain with consistent rewards

Cause 5: Simply needs more training
────────────────────────────────────
Checkers is complex!
With LR=1e-6, might need 20k-50k episodes

Fix: Train longer, be patient

═══════════════════════════════════════════════════════════════════════════
🔧 NUCLEAR OPTION: If Nothing Works
═══════════════════════════════════════════════════════════════════════════

If you've tried everything and it still fails:

Step 1: Verify Environment
───────────────────────────
Test that CheckersEnv works correctly:
  - Legal moves are actually legal
  - Rewards are sensible
  - Game terminates properly
  - State representation is correct

Step 2: Test with Simple Network
─────────────────────────────────
Replace Dueling DQN with vanilla DQN:
  Just value stream, no advantage
  If this works → Problem with Dueling architecture
  If this fails → Problem elsewhere

Step 3: Test with Smaller Action Space
───────────────────────────────────────
Reduce to top-N most common actions (e.g., 50)
  If this works → 170 actions is the bottleneck
  If this fails → Problem not action space

Step 4: Train on Simpler Task First
────────────────────────────────────
Before full checkers:
  - Train on endgame positions (3-4 pieces)
  - Train on capturing sequences only
  - Train on promotion to king

If these work → Need curriculum learning
If these fail → Fundamental architecture issue

Step 5: Compare with Baseline
──────────────────────────────
Implement simple Q-learning (table-based) on tiny state space
  If baseline works → Network is the issue
  If baseline fails → Reward/environment issue

═══════════════════════════════════════════════════════════════════════════
📊 MONITORING CHECKLIST
═══════════════════════════════════════════════════════════════════════════

Every 100 Episodes, Check:
───────────────────────────
□ Q-value range: Should be in [-30, +30]
□ Loss value: Should be decreasing
□ Episode reward: Should be increasing (slowly)
□ Gradient norms: Should be < 1.0
□ Feature statistics: mean≈0, std≈1 after LayerNorm

Every 1000 Episodes, Evaluate:
───────────────────────────────
□ Win rate vs random (10 games)
□ Average episode length
□ Capture percentage (captures / total moves)
□ King creation rate

Red Flags (Stop Training!):
────────────────────────────
🚨 Q-values exceed ±50
🚨 Loss exceeds 100
🚨 NaN or Inf anywhere
🚨 All Q-values identical (no differentiation)
🚨 Gradients all zero (no learning)

═══════════════════════════════════════════════════════════════════════════
💡 WHEN TO ADJUST WHAT
═══════════════════════════════════════════════════════════════════════════

If Q-values too high (>30):
  → Reduce LR or increase output scaling (×0.05)

If Q-values too low (<5):
  → Increase LR or reduce output scaling (×0.2)

If loss too high (>20 after 1000 ep):
  → Check Q-values first (probably exploding)
  → Reduce Huber delta if Q-values fine

If not learning (flat rewards):
  → Increase LR (if stable)
  → Increase exploration (higher ε_end)
  → Check reward signal (is agent winning ever?)

If learning but losing to random:
  → Train longer (double episodes)
  → Reduce scaling (both output and reward)
  → Add intermediate rewards

If unstable (oscillating):
  → Reduce LR
  → Increase target update frequency
  → Increase batch size

═══════════════════════════════════════════════════════════════════════════
✅ SUCCESS CRITERIA
═══════════════════════════════════════════════════════════════════════════

After 10,000 Episodes, You Should See:
───────────────────────────────────────
✓ Q-values: Stable in [-20, +20] range
✓ Loss: < 5.0, still slowly decreasing
✓ Episode reward: > +0.8 average
✓ Win rate vs random: > 85%
✓ Capture percentage: > 70%
✓ King creation: Most games
✓ No NaN/Inf: Clean training logs

If All Criteria Met:
────────────────────
🎉 Training successful!
→ Proceed to play against stronger opponents
→ Consider fine-tuning with self-play
→ Deploy as baseline agent

If Some Criteria Missing:
──────────────────────────
→ Train longer (20k episodes)
→ Adjust hyperparameters per guidelines above
→ Consider curriculum learning approach

═══════════════════════════════════════════════════════════════════════════
